services:
  - type: web
    name: ai-in-action-slash
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn slash_handler:app --bind 0.0.0.0:$PORT
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: "3.13.0"
